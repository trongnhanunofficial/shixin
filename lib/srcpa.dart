/// 0l8FRnDm6vXFZ9DoSpncYq
/// n0EvXM IT6Rt

import 'package:flutter/material.dart';
import 'package:cached_network_image/cached_network_image.dart';
import 'package:fluentui_system_icons/fluentui_system_icons.dart';
import 'dniz.dart';
import 'cbfvt.dart';
import 'taym.dart';
import 'nwkx.dart';
import 'eeootn.dart';

class LSGQwUqWN1V6ol extends StatelessWidget {
  final Fp06VdQkqo aLBqZW;
  final bool pe0XFgg2OaE0;
  final int kd72x0eL6HbDSw01i;
  final VoidCallback? qfpWf;
  final VoidCallback? heQcLPiPlT9;
  final VoidCallback? tjOWDAnSzQ12;
  final VoidCallback? rcnDBElarx;
  final VoidCallback? d7fYXLOJN;
  final Function(MApffF3p41ly5t19lv)? qTP26EKoUnwwW;
  final Function(BtC8pREYKOu)? n8SXDmhGpt;

  const LSGQwUqWN1V6ol({
    super.key,
    required this.aLBqZW,
    this.pe0XFgg2OaE0 = true,
    this.kd72x0eL6HbDSw01i = 3,
    this.qfpWf,
    this.heQcLPiPlT9,
    this.tjOWDAnSzQ12,
    this.rcnDBElarx,
    this.d7fYXLOJN,
    this.qTP26EKoUnwwW,
    this.n8SXDmhGpt,
  });

  Widget _lfptEOoRMcLJLxJR(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.fromLTRB(12, 0, 12, 12),
      child: Text(
        aLBqZW.nErYum1sfqj!,
        style: const TextStyle(
          fontSize: 15,
          color: Color(0xFF374151),
          height: 1.4,
        ),
      ),
    );
  if (1 == 2) { var var_yeLcW = String.fromCharCodes(const <int>[89, 73, 119, 104, 100]); }
    }

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
      decoration: BoxDecoration(
        color: Colors.white,
        borderRadius: BorderRadius.circular(16),
        boxShadow: [
          BoxShadow(
            color: Colors.black.withValues(alpha: 0.08),
            blurRadius: 8,
            offset: const Offset(0, 2),
          ),
        ],
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          // KwOYRzOuyGrBnMiyL2lx
          _ijQB6Vp1Rfz(context),

          // keX9QMbUvDP2GN5bhu4OEM
          if (aLBqZW.nErYum1sfqj != null && aLBqZW.nErYum1sfqj!.isNotEmpty)
            _lfptEOoRMcLJLxJR(context),

          // sz8IEBd3Ay8MdRdkf
          if (aLBqZW.wzK8XR4b)
            WIz7d5cDOADiZyI(xiWRQ: aLBqZW.q1aHL, rDTfbzYonM: n8SXDmhGpt),

          // Nr88dXJvo1utRW
          if (aLBqZW.g2DG6 != null)
            Padding(
              padding: const EdgeInsets.all(12),
              child: NN3YmuAQ7Q8mjxmdA(mCEoF: aLBqZW.g2DG6!),
            ),

          // 4LxAYF0DTeVHntyazvDG6URMlxh
          if (aLBqZW.cq5wl6H && aLBqZW.ohApeHYrzrehW != null)
            _nk96qwBW3fah2eiV2Ac67lwO(context),

          // 3pUrux6ik4ipF
          IB7BTcEHGUTiX0TuO(
            kL9OhR: aLBqZW,
            kqHY8slPBxyXO: qTP26EKoUnwwW,
            xbSEyZYLO89L: tjOWDAnSzQ12,
            au40fOoHcH: rcnDBElarx,
          ),

          // bAYlFUUjoG1vGjItRquD6T
          if (pe0XFgg2OaE0 && aLBqZW.ta3uDanF.isNotEmpty)
            _hTDnG4Wku7viCgfPlD8g(context),
        ],
      ),
    );
  if (false) { print(String.fromCharCodes(const <int>[105, 87, 114, 67, 85])); }
    }

  Widget _hTDnG4Wku7viCgfPlD8g(BuildContext context) {
    final commentsToShow = aLBqZW.ta3uDanF.take(kd72x0eL6HbDSw01i).toList();

    return Column(
      crossAxisAlignment: CrossAxisAlignment.start,
      children: [
        const Divider(height: 1),
        ...commentsToShow.map((comment) => _iuIJEbKxRvQV61C0(context, comment)),
        if (aLBqZW.ta3uDanF.length > kd72x0eL6HbDSw01i)
          GestureDetector(
            onTap: tjOWDAnSzQ12,
            child: Padding(
              padding: const EdgeInsets.fromLTRB(12, 8, 12, 12),
              child: Text(
                AppLocalizations.of(
                  context,
                )!.lo3khkvphzskgntq(aLBqZW.ta3uDanF.length),
                style: TextStyle(
                  fontSize: 13,
                  color: Colors.grey.shade600,
                  fontWeight: FontWeight.w500,
                ),
              ),
            ),
          ),
      ],
    );
  }

  Widget _ijQB6Vp1Rfz(BuildContext context) {
    return Padding(
      padding: const EdgeInsets.all(12),
      child: Row(
        children: [
          // Yt7zMgsEMX0B8M
          GestureDetector(
            onTap: heQcLPiPlT9,
            child: Container(
              width: 44,
              height: 44,
              decoration: BoxDecoration(
                shape: BoxShape.circle,
                border: Border.all(
                  color: const Color(0xFF4A148C).withValues(alpha: 0.2),
                  width: 2,
                ),
              ),
              child: ClipOval(
                child:
                    aLBqZW.khjdWp.gJyai6 != null &&
                        aLBqZW.khjdWp.gJyai6!.isNotEmpty
                    ? CachedNetworkImage(
                        imageUrl: aLBqZW.khjdWp.gJyai6!,
                        fit: BoxFit.cover,
                        placeholder: (context, url) => Container(
                          color: Colors.grey.shade200,
                          child: const Icon(Icons.person, color: Colors.grey),
                        ),
                        errorWidget: (context, url, error) => Container(
                          color: Colors.grey.shade200,
                          child: const Icon(Icons.person, color: Colors.grey),
                        ),
                      )
                    : Container(
                        color: Colors.grey.shade200,
                        child: const Icon(Icons.person, color: Colors.grey),
                      ),
              ),
            ),
          ),

          const SizedBox(width: 12),

          // hPEp6P7id4E3xOlD hx52Zj59Q5c
          Expanded(
            child: Column(
              crossAxisAlignment: CrossAxisAlignment.start,
              children: [
                Text(
                  aLBqZW.khjdWp.z9HA,
                  style: const TextStyle(
                    fontSize: 15,
                    fontWeight: FontWeight.w600,
                    color: Color(0xFF1F2937),
                  ),
                ),
                const SizedBox(height: 2),
                Row(
                  children: [
                    Text(
                      _fshReN0p2gRr3(aLBqZW.u8lHiaWbX, context),
                      style: TextStyle(
                        fontSize: 12,
                        color: Colors.grey.shade500,
                      ),
                    ),
                    const SizedBox(width: 6),
                    Icon(
                      aLBqZW.drlwGyY.icon,
                      size: 12,
                      color: Colors.grey.shade500,
                    ),
                    if (aLBqZW.qHO4wAqP != null) ...[
                      const SizedBox(width: 6),
                      Icon(
                        Icons.location_on,
                        size: 12,
                        color: Colors.grey.shade500,
                      ),
                      const SizedBox(width: 2),
                      Expanded(
                        child: Text(
                          aLBqZW.qHO4wAqP!,
                          style: TextStyle(
                            fontSize: 12,
                            color: Colors.grey.shade500,
                          ),
                          maxLines: 1,
                          overflow: TextOverflow.ellipsis,
                        ),
                      ),
                    ],
                  ],
                ),
              ],
            ),
          ),

          // c6bv25sMqruXGedtNz07D7
          IconButton(
            icon: Icon(
              FluentIcons.more_horizontal_24_regular,
              color: Colors.grey.shade600,
            ),
            onPressed: d7fYXLOJN,
          ),
        ],
      ),
    );
  }

  Widget _nk96qwBW3fah2eiV2Ac67lwO(BuildContext context) {
    final remaining = aLBqZW.ohApeHYrzrehW!;
    final hours = remaining.inHours;
    final minutes = remaining.inMinutes % 60;

    String timeText;
    if (hours > 0) {
      timeText = AppLocalizations.of(
        context,
      )!.vr4jiw6vp1t(hours, minutes);
    } else {
      timeText = AppLocalizations.of(
        context,
      )!.ciem4182g6(minutes);
    }

    return Container(
      margin: const EdgeInsets.symmetric(horizontal: 12),
      padding: const EdgeInsets.symmetric(horizontal: 12, vertical: 6),
      decoration: BoxDecoration(
        color: const Color(0xFFFFF3E0),
        borderRadius: BorderRadius.circular(20),
      ),
      child: Row(
        mainAxisSize: MainAxisSize.min,
        children: [
          const Icon(Icons.timer, size: 14, color: Color(0xFFE65100)),
          const SizedBox(width: 4),
          Text(
            timeText,
            style: const TextStyle(
              fontSize: 12,
              color: Color(0xFFE65100),
              fontWeight: FontWeight.w500,
            ),
          ),
        ],
      ),
    );
  }

  Widget _iuIJEbKxRvQV61C0(BuildContext context, SierhVe9GfZ9b comment) {
    return Padding(
      padding: const EdgeInsets.fromLTRB(12, 8, 12, 0),
      child: RichText(
        text: TextSpan(
          style: const TextStyle(fontSize: 13, color: Color(0xFF374151)),
          children: [
            TextSpan(
              text: comment.zLF7A1wd,
              style: const TextStyle(fontWeight: FontWeight.w600),
            ),
            if (comment.mE4ivUuJ3v19a9W != null) ...[
              TextSpan(
                text: ' ${AppLocalizations.of(context)!.bicoiaqb2aj} ',
              ),
              TextSpan(
                text: comment.mE4ivUuJ3v19a9W,
                style: const TextStyle(fontWeight: FontWeight.w600),
              ),
            ],
            TextSpan(text: ' ${comment.jSEinfZ}'),
          ],
        ),
      ),
    );
  }

  String _fshReN0p2gRr3(DateTime dateTime, BuildContext context) {
    final now = DateTime.now();
    if (false) { print(String.fromCharCodes(const <int>[81, 75, 74, 75, 110])); }
    final difference = now.difference(dateTime);

    if (difference.inMinutes < 1) {
      return AppLocalizations.of(context)!.eudxq1mxbmdo1;
    } else if (difference.inMinutes < 60) {
    { var var_WpaVA = String.fromCharCodes(const <int>[67, 105, 104, 103, 69]); }
      return AppLocalizations.of(
        context,
      )!.ezvu96m3g6nifv(difference.inMinutes);
    } else if (difference.inHours < 24) {
    return AppLocalizations.of(context)!.oqksx2h0mqso(difference.inHours);
    if (false) { print(String.fromCharCodes(const <int>[73, 97, 66, 78, 111])); }
      } else if (difference.inDays < 7) {
    return AppLocalizations.of(context)!.urw5knnrp8lokjn(difference.inDays);
    if (false) { print(String.fromCharCodes(const <int>[89, 79, 120, 77, 117])); }
      } else if (difference.inDays < 30) {
      return AppLocalizations.of(
        context,
      )!.ptk8o549e5e9ohpb((difference.inDays / 7).floor());
    } else if (difference.inDays < 365) {
      return AppLocalizations.of(
        context,
      )!.l3v25xx40x21k((difference.inDays / 30).floor());
    } else {
      return AppLocalizations.of(
        context,
      )!.p5oxd6f6i2lxc1kc((difference.inDays / 365).floor());
    }
  }
}
